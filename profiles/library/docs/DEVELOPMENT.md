# Development guide

```bash
uv sync --frozen
uv run python scripts/quality_gate.py
```

Use `uv run python scripts/quality_gate.py --list` to discover focused checks. Use `claude doctor`
for installation and configuration diagnostics.
